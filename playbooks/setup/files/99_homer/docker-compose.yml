services:
  homer:
    image: b4bz/homer
    container_name: homer
    volumes:
      - ${HOMER_ASSETS_DIR}:/www/assets # Make sure your local config directory exists
    ports:
      - 8080:8080
    environment:
      - INIT_ASSETS=0
    restart: unless-stopped
